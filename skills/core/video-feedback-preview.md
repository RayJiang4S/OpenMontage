# Video Feedback Preview

Compatibility note: `video_feedback_preview` still works for older projects,
but the standard OpenMontage capability is now
`video_feedback_review_package`.

Use `skills/core/video-feedback-review-package.md` for current instructions.
